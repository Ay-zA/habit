import React from 'react';

const About = () => (
  <div>
    <h1 className="title">
      Hello from
      <span>React + Mongo + Express + Node + Webpack + HMR + ES7 + ESLint</span>
    </h1>
  </div>
);

export default About;
